import React from 'react';
import { Article } from '../components/Article/Article';

const ArticleDetailView: React.FC = props => {
  return <Article {...props} />;
};

export default ArticleDetailView;
